import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({

    homepage: {
        marginTop: "10em",
        display: "flex",
        justifyContent: "center",
        height:"43em"
    },
}))